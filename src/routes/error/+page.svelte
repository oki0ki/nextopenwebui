<script>
        import { goto } from '$app/navigation';
        import { WEBUI_NAME, config } from '$lib/stores';
        import { onMount } from 'svelte';

        let loaded = false;

        onMount(async () => {
                if ($config) {
                        await goto('/');
                }

                loaded = true;
        });
</script>

{#if loaded}
        <div class="absolute w-full h-full flex z-50">
                <div class="absolute rounded-xl w-full h-full backdrop-blur flex justify-center">
                        <div class="m-auto pb-44 flex flex-col justify-center">
                                <div class="max-w-md">
                                        <div class="text-center text-2xl font-medium z-50">
                                                {`Backend ${$WEBUI_NAME} wymagane`}
                                        </div>

                                        <div class=" mt-4 text-center text-sm w-full">
                                                {"Oops! You're using an unsupported method (frontend only). Please serve the WebUI from the backend."}

                                                <br class=" " />
                                                <br class=" " />
                                                <a
                                                        class=" font-normal underline"
                                                        href="https://github.com/open-webui/open-webui#how-to-install-"
                                                        target="_blank">{'Zajrzyj do readme.md po instrukcje'}</a
                                                >
                                                {'lub'}
                                                <a class=" font-normal underline" href="https://discord.gg/5rJgQTnV4s" target="_blank"
                                                        >{'Dołącz do naszego Discorda po pomoc.'}</a
                                                >
                                        </div>

                                        <div class=" mt-6 mx-auto relative group w-fit">
                                                <button
                                                        class="relative z-20 flex px-5 py-2 rounded-full bg-gray-100 hover:bg-gray-200 transition font-medium text-sm"
                                                        on:click={() => {
                                                                location.href = '/';
                                                        }}
                                                >
                                                        {'Sprawdź ponownie'}
                                                </button>
                                        </div>
                                </div>
                        </div>
                </div>
        </div>
{/if}
